#include<stdio.h>
void main()
{
char ch:
printf("the character is ");
scanf("%s",&ch);
if(ch>=a && ch<=z)
printf("the chracter is alphabet");
else
printf("the given charcter is not alphabet");
getch();
}

